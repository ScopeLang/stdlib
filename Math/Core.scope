namespace Math;

/%
@asm

Returns the square root of @"x".
%/
func dec sqrt(dec x) {
	dec o = 0.0;
	assembly {
		vlist_get rdi, $x$
		mov QWORD [fptmp], rdi
		fld QWORD [fptmp]
		fsqrt
		fstp QWORD [fptmp]
		mov rdi, QWORD [fptmp]
		vlist_set $o$
	}
	ret o;
}