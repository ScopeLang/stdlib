func void println(str s) {
	print(s + "\n");
}

func str boolToStr(bool b) {
	if (b) {
		ret "true";
	}
	ret "false";
}

func str intToStr(int num) {
	// If we don't do this, this will return ""
	if (num == 0) {
		ret "0";
	}
	
	int n = num;
	str out = "";
	bool neg = n < 0;
	
	// Deal with negatives
	if (neg) {
		n = 0 - n;
	}
	
	// Add digits (in reverse order)
	while (n > 0) {
		out = "0123456789"[n % 10] + out;
		n = n / 10;
	}
	
	// Add negative sign (if negative)
	if (neg) {
		out = "-" + out;
	}
	
	ret out;
}